version: "3.8"

services:
  db:
    image: mysql:8.0
    container_name: cyber_mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=CHIdex20042605@
      - MYSQL_DATABASE=cyber_threat_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
